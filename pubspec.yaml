name: online_sessions
description: "Managing online sessions for VTTs."
version: 0.1.25
homepage:

publish_to: none

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  cloud_firestore: ^5.3.0
  collection: ^1.17.0
  crypto: ^3.0.6
  cubit_pool:
    git:
      url: https://github.com/andrewsheridan/cubit_pool
      ref: 0.3.3
    # path: "../cubit_pool"
  firebase_auth: ^5.1.2
  firebase_core: ^3.2.0
  firebase_storage: ^12.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.0.0
  google_sign_in: ^6.2.2
  hydrated_bloc: ^10.0.0
  logging: ^1.0.2
  sign_in_with_apple: ^7.0.1

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.1.7
  flutter_gen: ^5.4.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed: ^3.0.6
  json_serializable: ^6.1.6
  mocktail: ^1.0.0
  source_gen: ^2.0.0

flutter:
